{
  "name": "audio-translator-app",
  "version": "1.0.0",
  "description": "A static web application that translates English audio files to French using Azure Cognitive Services",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=3000 --open=index.html",
    "dev": "live-server --port=3000 --open=index.html --watch=.",
    "build": "echo 'Static app - no build process required'",
    "serve": "python -m http.server 3000"
  },
  "keywords": [
    "azure",
    "speech-to-text",
    "translator",
    "text-to-speech",
    "audio",
    "translation",
    "french",
    "english",
    "static-web-app"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/audio-translator-app.git"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
